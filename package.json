{
  "name": "node-handlebars-boilerplate",
  "version": "1.0.0",
  "description": "Project 3node",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npm run lint && cross-env NODE_ENV=test mocha **/*.test.js -u tdd --reporter spec --exit",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "bcrypt": "^3.0.5",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "express-handlebars": "^3.1.0",
    "express-session": "^1.15.6",
    "mysql2": "^1.6.4",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "sequelize": "^4.41.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "cross-env": "^5.2.0",
    "mocha": "^5.2.0"
  },
  "engines": {
    "node": "8.11.4"
  }
}
